# Dépendances du pipeline de données Charlie Investissement.
# Miroir de scripts/setup.sh — utilisé par les workflows GitHub Actions
# (weekly-refresh / monthly-enrich) pour une installation reproductible.
supabase
pdfplumber
requests
anthropic
python-dotenv
yfinance
pandas
numpy
parsel  # parsing HTML/CSS (scpi-primaliance-enricher) — remplace scrapling en CI
